{
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.4",
    "pg": "^8.13.1"
  },
  "description": "Infinite collaborative drawing canvas for two \u2014 password-protected rooms, real-time sync, persistent strokes",
  "devDependencies": {
    "socket.io-client": "^4.8.4"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "ISC",
  "main": "server.js",
  "name": "our-canvas",
  "scripts": {
    "start": "node server.js",
    "test": "node scripts/simulate.js && node scripts/text-geometry-test.js && node scripts/otp-boxes-test.js && node scripts/persistence-flush-test.js"
  },
  "version": "1.0.0"
}